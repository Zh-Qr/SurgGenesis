# SurgGenesis — Python dependencies
# Tested with Python 3.10, CUDA 12.x, on 2x A100-40GB.
#
# NOTE: this project also requires two upstream codebases that are NOT on PyPI:
#   - WAN2.2 / Wan2.1 backbone  (provides the `wan` package)   https://github.com/Wan-Video
#   - DiffSynth-Studio          (the LoRA trainer)             https://github.com/modelscope/DiffSynth-Studio
# Install those separately and point configs/*.env at them (DIFFSYNTH_ROOT, WAN_ROOT, CKPT_DIR).

torch>=2.4.0
torchvision>=0.19.0
torchaudio
numpy>=1.23.5,<2

# diffusion / model stack
diffusers>=0.31.0
transformers>=4.49.0,<=4.51.3
tokenizers>=0.20.3
accelerate>=1.1.1
safetensors

# video / image I/O
opencv-python>=4.9.0.80
imageio[ffmpeg]
imageio-ffmpeg
Pillow
einops
ftfy
easydict

# metrics / analysis / plotting
scipy
pandas
matplotlib
scikit-image
tqdm

# optional, for faster attention (recommended, may require manual build)
# flash-attn
